<odoo>
    <!-- Vista de lista (tree) -->
    <record id="view_campana_espia_tree" model="ir.ui.view">
        <field name="name">igb_barrasa_system.campana_espia.tree</field>
        <field name="model">igb_barrasa_system.campana_espia</field>
        <field name="arch" type="xml">
            <tree>
                <field name="id_espia"/>
                <field name="nombre"/>
                <field name="fecha_creacion"/>
            </tree>
        </field>
    </record>

    <!-- Vista de formulario (form) -->
    <record id="view_campana_espia_form" model="ir.ui.view">
        <field name="name">igb_barrasa_system.campana_espia.form</field>
        <field name="model">igb_barrasa_system.campana_espia</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="nombre"/>
                        <field name="imagen" widget="image"/>
                        <field name="fecha_creacion"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción de la vista de campañas -->
    <record id="action_campana_espia" model="ir.actions.act_window">
        <field name="name">Espía Web</field>
        <field name="res_model">igb_barrasa_system.campana_espia</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Menú principal "Espía Web" -->
    <menuitem id="menu_espia_web_root" name="Espía Web" sequence="1" groups="igb_barrasa_system_user" action="igb_barrasa_system.action_campana_espia"/>
</odoo>